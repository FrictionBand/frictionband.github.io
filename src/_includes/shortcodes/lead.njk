{# Lead paragraph shortcode template. Accepts `text` or `content` variables. #}
{% set leadText = text or content or '' %}
<p class="text-lg md:text-xl font-semibold text-neural-100 italic leading-tight mb-12">{{ leadText | safe }}</p>
